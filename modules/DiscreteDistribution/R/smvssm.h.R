
# This file is automatically generated, you probably don't want to edit this

SMVSSMOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SMVSSMOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            x1 = NULL,
            x2 = NULL,
            n1 = NULL,
            nneq = TRUE,
            n2 = NULL,
            a = "0.0250", ...) {

            super$initialize(
                package="DiscreteDistribution",
                name="SMVSSM",
                requiresData=TRUE,
                ...)

            private$..x1 <- jmvcore::OptionNumber$new(
                "x1",
                x1)
            private$..x2 <- jmvcore::OptionNumber$new(
                "x2",
                x2)
            private$..n1 <- jmvcore::OptionInteger$new(
                "n1",
                n1)
            private$..nneq <- jmvcore::OptionBool$new(
                "nneq",
                nneq,
                default=TRUE)
            private$..n2 <- jmvcore::OptionInteger$new(
                "n2",
                n2)
            private$..a <- jmvcore::OptionList$new(
                "a",
                a,
                options=list(
                    "0.0050",
                    "0.0250",
                    "0.050"),
                default="0.0250")

            self$.addOption(private$..x1)
            self$.addOption(private$..x2)
            self$.addOption(private$..n1)
            self$.addOption(private$..nneq)
            self$.addOption(private$..n2)
            self$.addOption(private$..a)
        }),
    active = list(
        x1 = function() private$..x1$value,
        x2 = function() private$..x2$value,
        n1 = function() private$..n1$value,
        nneq = function() private$..nneq$value,
        n2 = function() private$..n2$value,
        a = function() private$..a$value),
    private = list(
        ..x1 = NA,
        ..x2 = NA,
        ..n1 = NA,
        ..nneq = NA,
        ..n2 = NA,
        ..a = NA)
)

SMVSSMResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SMVSSMResults",
    inherit = jmvcore::Group,
    active = list(
        h0 = function() private$.items[["h0"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Sample Mean VS Sample Mean(Possion)")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="h0",
                title=""))}))

SMVSSMBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SMVSSMBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "DiscreteDistribution",
                name = "SMVSSM",
                version = c(1,0,0),
                options = options,
                results = SMVSSMResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Sample Mean VS Sample Mean(Possion)
#'
#' 
#' @param data .
#' @param x1 .
#' @param x2 .
#' @param n1 .
#' @param nneq .
#' @param n2 .
#' @param a .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$h0} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
SMVSSM <- function(
    data,
    x1,
    x2,
    n1,
    nneq = TRUE,
    n2,
    a = "0.0250") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("SMVSSM requires jmvcore to be installed (restart may be required)")

    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame())


    options <- SMVSSMOptions$new(
        x1 = x1,
        x2 = x2,
        n1 = n1,
        nneq = nneq,
        n2 = n2,
        a = a)

    analysis <- SMVSSMClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

