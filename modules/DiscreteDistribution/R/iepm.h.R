
# This file is automatically generated, you probably don't want to edit this

IEPMOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "IEPMOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            nam = NULL,
            x = NULL,
            a = "0.0250", ...) {

            super$initialize(
                package="DiscreteDistribution",
                name="IEPM",
                requiresData=TRUE,
                ...)

            private$..nam <- jmvcore::OptionBool$new(
                "nam",
                nam)
            private$..x <- jmvcore::OptionInteger$new(
                "x",
                x)
            private$..a <- jmvcore::OptionList$new(
                "a",
                a,
                options=list(
                    "0.0050",
                    "0.0250",
                    "0.050"),
                default="0.0250")

            self$.addOption(private$..nam)
            self$.addOption(private$..x)
            self$.addOption(private$..a)
        }),
    active = list(
        nam = function() private$..nam$value,
        x = function() private$..x$value,
        a = function() private$..a$value),
    private = list(
        ..nam = NA,
        ..x = NA,
        ..a = NA)
)

IEPMResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "IEPMResults",
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Interval Estimation of Population Mean")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Interval Estimation of Population Mean"))}))

IEPMBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "IEPMBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "DiscreteDistribution",
                name = "IEPM",
                version = c(1,0,0),
                options = options,
                results = IEPMResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Interval Estimation of Population Mean
#'
#' 
#' @param data .
#' @param nam .
#' @param x .
#' @param a .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
IEPM <- function(
    data,
    nam,
    x,
    a = "0.0250") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("IEPM requires jmvcore to be installed (restart may be required)")

    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame())


    options <- IEPMOptions$new(
        nam = nam,
        x = x,
        a = a)

    analysis <- IEPMClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

