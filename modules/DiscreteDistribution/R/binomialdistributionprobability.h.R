
# This file is automatically generated, you probably don't want to edit this

BinomialDistributionProbabilityOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "BinomialDistributionProbabilityOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            p = NULL,
            n = NULL,
            x = NULL, ...) {

            super$initialize(
                package="DiscreteDistribution",
                name="BinomialDistributionProbability",
                requiresData=TRUE,
                ...)

            private$..p <- jmvcore::OptionNumber$new(
                "p",
                p)
            private$..n <- jmvcore::OptionInteger$new(
                "n",
                n)
            private$..x <- jmvcore::OptionInteger$new(
                "x",
                x)

            self$.addOption(private$..p)
            self$.addOption(private$..n)
            self$.addOption(private$..x)
        }),
    active = list(
        p = function() private$..p$value,
        n = function() private$..n$value,
        x = function() private$..x$value),
    private = list(
        ..p = NA,
        ..n = NA,
        ..x = NA)
)

BinomialDistributionProbabilityResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "BinomialDistributionProbabilityResults",
    inherit = jmvcore::Group,
    active = list(
        p = function() private$.items[["p"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Binomial Distribution Probability")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="p",
                title=""))}))

BinomialDistributionProbabilityBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "BinomialDistributionProbabilityBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "DiscreteDistribution",
                name = "BinomialDistributionProbability",
                version = c(1,0,0),
                options = options,
                results = BinomialDistributionProbabilityResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Binomial Distribution Probability
#'
#' 
#' @param data .
#' @param p .
#' @param n .
#' @param x .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$p} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
BinomialDistributionProbability <- function(
    data,
    p,
    n,
    x) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("BinomialDistributionProbability requires jmvcore to be installed (restart may be required)")

    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame())


    options <- BinomialDistributionProbabilityOptions$new(
        p = p,
        n = n,
        x = x)

    analysis <- BinomialDistributionProbabilityClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

