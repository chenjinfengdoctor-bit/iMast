
# This file is automatically generated, you probably don't want to edit this

ReferenceRangePOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ReferenceRangePOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            l = NULL,
            i = NULL,
            f = NULL,
            rr = "95",
            side = "ts", ...) {

            super$initialize(
                package="Descriptive",
                name="ReferenceRangeP",
                requiresData=TRUE,
                ...)

            private$..l <- jmvcore::OptionVariable$new(
                "l",
                l)
            private$..i <- jmvcore::OptionVariable$new(
                "i",
                i)
            private$..f <- jmvcore::OptionVariable$new(
                "f",
                f)
            private$..rr <- jmvcore::OptionList$new(
                "rr",
                rr,
                default="95",
                options=list(
                    "99",
                    "95",
                    "90",
                    "80"))
            private$..side <- jmvcore::OptionList$new(
                "side",
                side,
                options=list(
                    "os",
                    "ts"),
                default="ts")

            self$.addOption(private$..l)
            self$.addOption(private$..i)
            self$.addOption(private$..f)
            self$.addOption(private$..rr)
            self$.addOption(private$..side)
        }),
    active = list(
        l = function() private$..l$value,
        i = function() private$..i$value,
        f = function() private$..f$value,
        rr = function() private$..rr$value,
        side = function() private$..side$value),
    private = list(
        ..l = NA,
        ..i = NA,
        ..f = NA,
        ..rr = NA,
        ..side = NA)
)

ReferenceRangePResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ReferenceRangePResults",
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Reference Range(Percentile Method)")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title=""))}))

ReferenceRangePBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ReferenceRangePBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "ReferenceRangeP",
                version = c(1,0,0),
                options = options,
                results = ReferenceRangePResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Reference Range(Percentile Method)
#'
#' 
#' @param data .
#' @param l .
#' @param i .
#' @param f .
#' @param rr .
#' @param side .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
ReferenceRangeP <- function(
    data,
    l,
    i,
    f,
    rr = "95",
    side = "ts") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("ReferenceRangeP requires jmvcore to be installed (restart may be required)")

    if ( ! missing(l)) l <- jmvcore::resolveQuo(jmvcore::enquo(l))
    if ( ! missing(i)) i <- jmvcore::resolveQuo(jmvcore::enquo(i))
    if ( ! missing(f)) f <- jmvcore::resolveQuo(jmvcore::enquo(f))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(l), l, NULL),
            `if`( ! missing(i), i, NULL),
            `if`( ! missing(f), f, NULL))


    options <- ReferenceRangePOptions$new(
        l = l,
        i = i,
        f = f,
        rr = rr,
        side = side)

    analysis <- ReferenceRangePClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

