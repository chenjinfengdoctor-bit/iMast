
# This file is automatically generated, you probably don't want to edit this

ArithmeticMeanDirectOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ArithmeticMeanDirectOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            x = NULL, ...) {

            super$initialize(
                package="Descriptive",
                name="ArithmeticMeanDirect",
                requiresData=TRUE,
                ...)

            private$..x <- jmvcore::OptionVariables$new(
                "x",
                x)

            self$.addOption(private$..x)
        }),
    active = list(
        x = function() private$..x$value),
    private = list(
        ..x = NA)
)

ArithmeticMeanDirectResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ArithmeticMeanDirectResults",
    inherit = jmvcore::Group,
    active = list(
        mean = function() private$.items[["mean"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Arithmetic Mean(Direct Method)")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="mean",
                title="$\\overline\\{X\\}=\\frac\\{\\sum X\\}\\{n\\}$"))}))

ArithmeticMeanDirectBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ArithmeticMeanDirectBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "ArithmeticMeanDirect",
                version = c(1,0,0),
                options = options,
                results = ArithmeticMeanDirectResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Arithmetic Mean(Direct Method)
#'
#' 
#' @param data .
#' @param x .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$mean} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
ArithmeticMeanDirect <- function(
    data,
    x) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("ArithmeticMeanDirect requires jmvcore to be installed (restart may be required)")

    if ( ! missing(x)) x <- jmvcore::resolveQuo(jmvcore::enquo(x))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(x), x, NULL))


    options <- ArithmeticMeanDirectOptions$new(
        x = x)

    analysis <- ArithmeticMeanDirectClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

