
# This file is automatically generated, you probably don't want to edit this

SampleSizeContinuousOneSampleOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousOneSampleOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            psd = NULL,
            dd = NULL,
            aer = "0.05",
            ber = "0.05", ...) {

            super$initialize(
                package="Descriptive",
                name="SampleSizeContinuousOneSample",
                requiresData=TRUE,
                ...)

            private$..psd <- jmvcore::OptionNumber$new(
                "psd",
                psd)
            private$..dd <- jmvcore::OptionNumber$new(
                "dd",
                dd)
            private$..aer <- jmvcore::OptionList$new(
                "aer",
                aer,
                options=list(
                    "0.005",
                    "0.01",
                    "0.025",
                    "0.05",
                    "0.0050",
                    "0.010",
                    "0.0250",
                    "0.050"),
                default="0.05")
            private$..ber <- jmvcore::OptionList$new(
                "ber",
                ber,
                options=list(
                    "0.01",
                    "0.05",
                    "0.10",
                    "0.20",
                    "0.5"),
                default="0.05")

            self$.addOption(private$..psd)
            self$.addOption(private$..dd)
            self$.addOption(private$..aer)
            self$.addOption(private$..ber)
        }),
    active = list(
        psd = function() private$..psd$value,
        dd = function() private$..dd$value,
        aer = function() private$..aer$value,
        ber = function() private$..ber$value),
    private = list(
        ..psd = NA,
        ..dd = NA,
        ..aer = NA,
        ..ber = NA)
)

SampleSizeContinuousOneSampleResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousOneSampleResults",
    inherit = jmvcore::Group,
    active = list(
        n = function() private$.items[["n"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Sample Size(Continuous One Sample)")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="n",
                title="Sample Size(Continuous One Sample)"))}))

SampleSizeContinuousOneSampleBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousOneSampleBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "SampleSizeContinuousOneSample",
                version = c(1,0,0),
                options = options,
                results = SampleSizeContinuousOneSampleResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Sample Size(Continuous One Sample)
#'
#' 
#' @param data .
#' @param psd .
#' @param dd .
#' @param aer .
#' @param ber .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$n} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
SampleSizeContinuousOneSample <- function(
    data,
    psd,
    dd,
    aer = "0.05",
    ber = "0.05") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("SampleSizeContinuousOneSample requires jmvcore to be installed (restart may be required)")

    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame())


    options <- SampleSizeContinuousOneSampleOptions$new(
        psd = psd,
        dd = dd,
        aer = aer,
        ber = ber)

    analysis <- SampleSizeContinuousOneSampleClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

