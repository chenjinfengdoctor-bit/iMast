
# This file is automatically generated, you probably don't want to edit this

SampleSizeContinuousTISOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousTISOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            psd = NULL,
            rdm = NULL,
            aer = "0.05",
            ber = "0.05", ...) {

            super$initialize(
                package="Descriptive",
                name="SampleSizeContinuousTIS",
                requiresData=TRUE,
                ...)

            private$..psd <- jmvcore::OptionNumber$new(
                "psd",
                psd)
            private$..rdm <- jmvcore::OptionNumber$new(
                "rdm",
                rdm)
            private$..aer <- jmvcore::OptionList$new(
                "aer",
                aer,
                options=list(
                    "0.005",
                    "0.01",
                    "0.025",
                    "0.05",
                    "0.0050",
                    "0.010",
                    "0.0250",
                    "0.050"),
                default="0.05")
            private$..ber <- jmvcore::OptionList$new(
                "ber",
                ber,
                options=list(
                    "0.01",
                    "0.05",
                    "0.10",
                    "0.20",
                    "0.5"),
                default="0.05")

            self$.addOption(private$..psd)
            self$.addOption(private$..rdm)
            self$.addOption(private$..aer)
            self$.addOption(private$..ber)
        }),
    active = list(
        psd = function() private$..psd$value,
        rdm = function() private$..rdm$value,
        aer = function() private$..aer$value,
        ber = function() private$..ber$value),
    private = list(
        ..psd = NA,
        ..rdm = NA,
        ..aer = NA,
        ..ber = NA)
)

SampleSizeContinuousTISResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousTISResults",
    inherit = jmvcore::Group,
    active = list(
        n = function() private$.items[["n"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Sample Size(Continuous Two Independent Samples With Common Standard Deviation)")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="n",
                title="Sample Size(Continuous Two Independent Samples With Common Standard Deviation)"))}))

SampleSizeContinuousTISBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousTISBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "SampleSizeContinuousTIS",
                version = c(1,0,0),
                options = options,
                results = SampleSizeContinuousTISResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Sample Size(Continuous Two Independent Samples With Common Standard Deviation)
#'
#' 
#' @param data .
#' @param psd .
#' @param rdm .
#' @param aer .
#' @param ber .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$n} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
SampleSizeContinuousTIS <- function(
    data,
    psd,
    rdm,
    aer = "0.05",
    ber = "0.05") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("SampleSizeContinuousTIS requires jmvcore to be installed (restart may be required)")

    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame())


    options <- SampleSizeContinuousTISOptions$new(
        psd = psd,
        rdm = rdm,
        aer = aer,
        ber = ber)

    analysis <- SampleSizeContinuousTISClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

