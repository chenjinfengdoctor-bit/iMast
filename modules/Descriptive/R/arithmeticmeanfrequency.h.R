
# This file is automatically generated, you probably don't want to edit this

ArithmeticMeanFrequencyOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ArithmeticMeanFrequencyOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            f = NULL,
            X = NULL, ...) {

            super$initialize(
                package="Descriptive",
                name="ArithmeticMeanFrequency",
                requiresData=TRUE,
                ...)

            private$..f <- jmvcore::OptionVariable$new(
                "f",
                f)
            private$..X <- jmvcore::OptionVariable$new(
                "X",
                X)

            self$.addOption(private$..f)
            self$.addOption(private$..X)
        }),
    active = list(
        f = function() private$..f$value,
        X = function() private$..X$value),
    private = list(
        ..f = NA,
        ..X = NA)
)

ArithmeticMeanFrequencyResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ArithmeticMeanFrequencyResults",
    inherit = jmvcore::Group,
    active = list(
        mean = function() private$.items[["mean"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="ArithmeticMeanFrequency")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="mean",
                title="$\\overline\\{X\\}=\\frac\\{\\sum fX\\}\\{\\sum f\\}$"))}))

ArithmeticMeanFrequencyBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "ArithmeticMeanFrequencyBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "ArithmeticMeanFrequency",
                version = c(1,0,0),
                options = options,
                results = ArithmeticMeanFrequencyResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Arithmetic Mean(Frequency Table Method)
#'
#' 
#' @param data .
#' @param f .
#' @param X .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$mean} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
ArithmeticMeanFrequency <- function(
    data,
    f,
    X) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("ArithmeticMeanFrequency requires jmvcore to be installed (restart may be required)")

    if ( ! missing(f)) f <- jmvcore::resolveQuo(jmvcore::enquo(f))
    if ( ! missing(X)) X <- jmvcore::resolveQuo(jmvcore::enquo(X))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(f), f, NULL),
            `if`( ! missing(X), X, NULL))


    options <- ArithmeticMeanFrequencyOptions$new(
        f = f,
        X = X)

    analysis <- ArithmeticMeanFrequencyClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

