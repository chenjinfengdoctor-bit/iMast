
# This file is automatically generated, you probably don't want to edit this

RangeOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "RangeOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            X = NULL, ...) {

            super$initialize(
                package="Descriptive",
                name="Range",
                requiresData=TRUE,
                ...)

            private$..X <- jmvcore::OptionVariables$new(
                "X",
                X)

            self$.addOption(private$..X)
        }),
    active = list(
        X = function() private$..X$value),
    private = list(
        ..X = NA)
)

RangeResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "RangeResults",
    inherit = jmvcore::Group,
    active = list(
        r = function() private$.items[["r"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Range")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="r",
                title="$R$"))}))

RangeBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "RangeBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "Range",
                version = c(1,0,0),
                options = options,
                results = RangeResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Range
#'
#' 
#' @param data .
#' @param X .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$r} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
Range <- function(
    data,
    X) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("Range requires jmvcore to be installed (restart may be required)")

    if ( ! missing(X)) X <- jmvcore::resolveQuo(jmvcore::enquo(X))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(X), X, NULL))


    options <- RangeOptions$new(
        X = X)

    analysis <- RangeClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

