
# This file is automatically generated, you probably don't want to edit this

SampleSizeContinuousTPSOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousTPSOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            sdmd = NULL,
            md = NULL,
            aer = "0.05",
            ber = "0.05", ...) {

            super$initialize(
                package="Descriptive",
                name="SampleSizeContinuousTPS",
                requiresData=TRUE,
                ...)

            private$..sdmd <- jmvcore::OptionNumber$new(
                "sdmd",
                sdmd)
            private$..md <- jmvcore::OptionNumber$new(
                "md",
                md)
            private$..aer <- jmvcore::OptionList$new(
                "aer",
                aer,
                options=list(
                    "0.005",
                    "0.01",
                    "0.025",
                    "0.05",
                    "0.0050",
                    "0.010",
                    "0.0250",
                    "0.050"),
                default="0.05")
            private$..ber <- jmvcore::OptionList$new(
                "ber",
                ber,
                options=list(
                    "0.01",
                    "0.05",
                    "0.10",
                    "0.20",
                    "0.5"),
                default="0.05")

            self$.addOption(private$..sdmd)
            self$.addOption(private$..md)
            self$.addOption(private$..aer)
            self$.addOption(private$..ber)
        }),
    active = list(
        sdmd = function() private$..sdmd$value,
        md = function() private$..md$value,
        aer = function() private$..aer$value,
        ber = function() private$..ber$value),
    private = list(
        ..sdmd = NA,
        ..md = NA,
        ..aer = NA,
        ..ber = NA)
)

SampleSizeContinuousTPSResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousTPSResults",
    inherit = jmvcore::Group,
    active = list(
        n = function() private$.items[["n"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Sample Size(Continuous Two Paired Samples)")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="n",
                title="Sample Size(Continuous Two Paired Samples)"))}))

SampleSizeContinuousTPSBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "SampleSizeContinuousTPSBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "SampleSizeContinuousTPS",
                version = c(1,0,0),
                options = options,
                results = SampleSizeContinuousTPSResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Sample Size(Continuous Two Paired Samples)
#'
#' 
#' @param data .
#' @param sdmd .
#' @param md .
#' @param aer .
#' @param ber .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$n} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
SampleSizeContinuousTPS <- function(
    data,
    sdmd,
    md,
    aer = "0.05",
    ber = "0.05") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("SampleSizeContinuousTPS requires jmvcore to be installed (restart may be required)")

    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame())


    options <- SampleSizeContinuousTPSOptions$new(
        sdmd = sdmd,
        md = md,
        aer = aer,
        ber = ber)

    analysis <- SampleSizeContinuousTPSClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

