
# This file is automatically generated, you probably don't want to edit this

VarianceFrequencyOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "VarianceFrequencyOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            f = NULL,
            X = NULL,
            CV = TRUE, ...) {

            super$initialize(
                package="Descriptive",
                name="VarianceFrequency",
                requiresData=TRUE,
                ...)

            private$..f <- jmvcore::OptionVariable$new(
                "f",
                f)
            private$..X <- jmvcore::OptionVariable$new(
                "X",
                X)
            private$..CV <- jmvcore::OptionBool$new(
                "CV",
                CV,
                default=TRUE)

            self$.addOption(private$..f)
            self$.addOption(private$..X)
            self$.addOption(private$..CV)
        }),
    active = list(
        f = function() private$..f$value,
        X = function() private$..X$value,
        CV = function() private$..CV$value),
    private = list(
        ..f = NA,
        ..X = NA,
        ..CV = NA)
)

VarianceFrequencyResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "VarianceFrequencyResults",
    inherit = jmvcore::Group,
    active = list(
        S2 = function() private$.items[["S2"]],
        S = function() private$.items[["S"]],
        CV = function() private$.items[["CV"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Variance And Standard Deviation(Frequency Table Method)")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="S2",
                title="$S^2$"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="S",
                title="$S$"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="CV",
                title=""))}))

VarianceFrequencyBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "VarianceFrequencyBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "VarianceFrequency",
                version = c(1,0,0),
                options = options,
                results = VarianceFrequencyResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Variance And Standard Deviation(Frequency Table Method)
#'
#' 
#' @param data .
#' @param f .
#' @param X .
#' @param CV .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$S2} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$S} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$CV} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
VarianceFrequency <- function(
    data,
    f,
    X,
    CV = TRUE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("VarianceFrequency requires jmvcore to be installed (restart may be required)")

    if ( ! missing(f)) f <- jmvcore::resolveQuo(jmvcore::enquo(f))
    if ( ! missing(X)) X <- jmvcore::resolveQuo(jmvcore::enquo(X))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(f), f, NULL),
            `if`( ! missing(X), X, NULL))


    options <- VarianceFrequencyOptions$new(
        f = f,
        X = X,
        CV = CV)

    analysis <- VarianceFrequencyClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

