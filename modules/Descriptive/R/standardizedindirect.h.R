
# This file is automatically generated, you probably don't want to edit this

StandardizedIndirectOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "StandardizedIndirectOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            na = NULL,
            nb = NULL,
            p = NULL,
            ra = NULL,
            rb = NULL, ...) {

            super$initialize(
                package="Descriptive",
                name="StandardizedIndirect",
                requiresData=TRUE,
                ...)

            private$..na <- jmvcore::OptionVariable$new(
                "na",
                na)
            private$..nb <- jmvcore::OptionVariable$new(
                "nb",
                nb)
            private$..p <- jmvcore::OptionVariable$new(
                "p",
                p)
            private$..ra <- jmvcore::OptionNumber$new(
                "ra",
                ra)
            private$..rb <- jmvcore::OptionNumber$new(
                "rb",
                rb)

            self$.addOption(private$..na)
            self$.addOption(private$..nb)
            self$.addOption(private$..p)
            self$.addOption(private$..ra)
            self$.addOption(private$..rb)
        }),
    active = list(
        na = function() private$..na$value,
        nb = function() private$..nb$value,
        p = function() private$..p$value,
        ra = function() private$..ra$value,
        rb = function() private$..rb$value),
    private = list(
        ..na = NA,
        ..nb = NA,
        ..p = NA,
        ..ra = NA,
        ..rb = NA)
)

StandardizedIndirectResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "StandardizedIndirectResults",
    inherit = jmvcore::Group,
    active = list(
        ppa = function() private$.items[["ppa"]],
        ppb = function() private$.items[["ppb"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Standardized(Indirect Method)")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="ppa",
                title="$p'_A$"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="ppb",
                title="$p'_B$"))}))

StandardizedIndirectBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "StandardizedIndirectBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "Descriptive",
                name = "StandardizedIndirect",
                version = c(1,0,0),
                options = options,
                results = StandardizedIndirectResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Standardized(Indirect Method)
#'
#' 
#' @param data .
#' @param na .
#' @param nb .
#' @param p .
#' @param ra .
#' @param rb .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$ppa} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$ppb} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
StandardizedIndirect <- function(
    data,
    na,
    nb,
    p,
    ra,
    rb) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("StandardizedIndirect requires jmvcore to be installed (restart may be required)")

    if ( ! missing(na)) na <- jmvcore::resolveQuo(jmvcore::enquo(na))
    if ( ! missing(nb)) nb <- jmvcore::resolveQuo(jmvcore::enquo(nb))
    if ( ! missing(p)) p <- jmvcore::resolveQuo(jmvcore::enquo(p))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(na), na, NULL),
            `if`( ! missing(nb), nb, NULL),
            `if`( ! missing(p), p, NULL))


    options <- StandardizedIndirectOptions$new(
        na = na,
        nb = nb,
        p = p,
        ra = ra,
        rb = rb)

    analysis <- StandardizedIndirectClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

