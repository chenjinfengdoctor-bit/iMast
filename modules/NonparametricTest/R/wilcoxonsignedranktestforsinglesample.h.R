
# This file is automatically generated, you probably don't want to edit this

WilcoxonSignedRankTestForSingleSampleOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "WilcoxonSignedRankTestForSingleSampleOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            x = NULL,
            m0 = NULL,
            h1 = NULL,
            correct = TRUE,
            a = "0.05", ...) {

            super$initialize(
                package="NonparametricTest",
                name="WilcoxonSignedRankTestForSingleSample",
                requiresData=TRUE,
                ...)

            private$..x <- jmvcore::OptionVariable$new(
                "x",
                x)
            private$..m0 <- jmvcore::OptionNumber$new(
                "m0",
                m0)
            private$..h1 <- jmvcore::OptionList$new(
                "h1",
                h1,
                options=list(
                    "neq",
                    "lt",
                    "gt"))
            private$..correct <- jmvcore::OptionBool$new(
                "correct",
                correct,
                default=TRUE)
            private$..a <- jmvcore::OptionList$new(
                "a",
                a,
                options=list(
                    "0.01",
                    "0.05",
                    "0.10"),
                default="0.05")

            self$.addOption(private$..x)
            self$.addOption(private$..m0)
            self$.addOption(private$..h1)
            self$.addOption(private$..correct)
            self$.addOption(private$..a)
        }),
    active = list(
        x = function() private$..x$value,
        m0 = function() private$..m0$value,
        h1 = function() private$..h1$value,
        correct = function() private$..correct$value,
        a = function() private$..a$value),
    private = list(
        ..x = NA,
        ..m0 = NA,
        ..h1 = NA,
        ..correct = NA,
        ..a = NA)
)

WilcoxonSignedRankTestForSingleSampleResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "WilcoxonSignedRankTestForSingleSampleResults",
    inherit = jmvcore::Group,
    active = list(
        h0 = function() private$.items[["h0"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Wilcoxon Signed-Rank Test For Single Sample")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="h0",
                title=""))}))

WilcoxonSignedRankTestForSingleSampleBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "WilcoxonSignedRankTestForSingleSampleBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "NonparametricTest",
                name = "WilcoxonSignedRankTestForSingleSample",
                version = c(1,0,0),
                options = options,
                results = WilcoxonSignedRankTestForSingleSampleResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Wilcoxon Signed-Rank Test For Single Sample
#'
#' 
#' @param data .
#' @param x .
#' @param m0 .
#' @param h1 .
#' @param correct .
#' @param a .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$h0} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
WilcoxonSignedRankTestForSingleSample <- function(
    data,
    x,
    m0,
    h1,
    correct = TRUE,
    a = "0.05") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("WilcoxonSignedRankTestForSingleSample requires jmvcore to be installed (restart may be required)")

    if ( ! missing(x)) x <- jmvcore::resolveQuo(jmvcore::enquo(x))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(x), x, NULL))


    options <- WilcoxonSignedRankTestForSingleSampleOptions$new(
        x = x,
        m0 = m0,
        h1 = h1,
        correct = correct,
        a = a)

    analysis <- WilcoxonSignedRankTestForSingleSampleClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

