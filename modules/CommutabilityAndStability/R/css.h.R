
# This file is automatically generated, you probably don't want to edit this

CSSOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "CSSOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            x = NULL,
            y = NULL, ...) {

            super$initialize(
                package="CommutabilityAndStability",
                name="CSS",
                requiresData=TRUE,
                ...)

            private$..x <- jmvcore::OptionVariable$new(
                "x",
                x)
            private$..y <- jmvcore::OptionVariables$new(
                "y",
                y)

            self$.addOption(private$..x)
            self$.addOption(private$..y)
        }),
    active = list(
        x = function() private$..x$value,
        y = function() private$..y$value),
    private = list(
        ..x = NA,
        ..y = NA)
)

CSSResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "CSSResults",
    inherit = jmvcore::Group,
    active = list(
        css = function() private$.items[["css"]],
        t = function() private$.items[["t"]],
        sb1 = function() private$.items[["sb1"]],
        b1 = function() private$.items[["b1"]],
        s = function() private$.items[["s"]],
        xi = function() private$.items[["xi"]],
        xm = function() private$.items[["xm"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Cold storage stability")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="css",
                title="Cold storage stability"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="t",
                title="$t$: Time points"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="sb1",
                title="$s(b_1)$: Uncertainty of slope"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="b1",
                title="$b_1$: Intercept"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="s",
                title="$s$: Standard deviation of straight line"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="xi",
                title="$x_i$: Time point"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="xm",
                title="$\\\\overline x$: Mean of time point"))}))

CSSBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "CSSBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "CommutabilityAndStability",
                name = "CSS",
                version = c(1,0,0),
                options = options,
                results = CSSResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Cold storage stability
#'
#' 
#' @param data .
#' @param x .
#' @param y .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$css} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$t} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$sb1} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$b1} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$s} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$xi} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$xm} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
CSS <- function(
    data,
    x,
    y) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("CSS requires jmvcore to be installed (restart may be required)")

    if ( ! missing(x)) x <- jmvcore::resolveQuo(jmvcore::enquo(x))
    if ( ! missing(y)) y <- jmvcore::resolveQuo(jmvcore::enquo(y))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(x), x, NULL),
            `if`( ! missing(y), y, NULL))


    options <- CSSOptions$new(
        x = x,
        y = y)

    analysis <- CSSClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

